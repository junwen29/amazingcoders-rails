<%= render 'layouts/biz/dash' %>

<div class="contentWrapper">
  <div class="content">

    <!-- Flash Error Message -->
    <% if @feedback.errors.any? %>
        <div id="error_explanation" class="alert alert-error">
          <h2>
            <%= pluralize(@feedback.errors.count, "error") %> prohibited this venue from being saved:
          </h2>
          <ul>
            <% @feedback.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="form-style-10" style="max-width:60%">
      <h1> Submit Your Feedback!</h1>
      <%= render :partial => 'form/feedback_create_form' %>
    </div>

  </div>
</div>

<%= content_for :form_footer do %>
    <%= render :partial => 'layouts/form_style' %>
<% end %>