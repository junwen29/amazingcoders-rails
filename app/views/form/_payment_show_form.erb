<p>
  Plan subscribed:
  <% if (@payment.plan1)  %>
      <span class="label label-default">Deal Listing Service</span>
  <% end %>
</p>

<p>
  Add Ons:
  <% if (@payment.add_on1) %>
      <span class="label label-default">Push Notification</span>
  <% end %>
  <% if (@payment.add_on2) %>
      <span class="label label-default">Deal Statistics</span>
  <% end %>
  <% if (@payment.add_on3) %>
      <span class="label label-default">Aggregate Trends</span>
  <% end %>
</p>

<p>
  Start Date:
  <strong><%= @payment.start_date %></strong>
</p>

<p>
  Expiry Date:
  <strong><%= @payment.expiry_date %></strong>
</p>

<h3>
  Total Price:
  <strong><%= number_to_currency @payment.total_cost %></strong>
</h3>

<script src="https://checkout.stripe.com/checkout.js" class="stripe-button"
        data-key="<%= Rails.configuration.stripe[:publishable_key] %>"
        data-description="A month's subscription"
        data-amount="500"
        data-locale="auto"></script>